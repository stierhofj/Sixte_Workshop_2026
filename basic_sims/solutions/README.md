# Solution exercise for the basic simulations

This folder matches the files from the parent folder except with all variables set. It also shows how the other exercies in this repository are structured, including the `run` script that can be used to inspect all commands as they will be executed.